{block content}
	<div class="container-fluid">
		<h2>blog</h2>
		<div n:foreach="$posts as $post" class="post">
				<h4><a href="{link Post:show $post->id}">{$post->title}</a></h4>
				<div>{$post->content}</div>
				<span>{$post->created_at|date:'F,j,Y'}</span>
		</div>
	</div>
{/block}